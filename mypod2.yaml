apiVersion: v1
kind: Pod
metadata:
  name: my-pod
spec:
  containers:
    - name: my-container
      image: my-image
      volumeMounts:
        - name: my-pvc-mount
          mountPath: \pathII
  volumes:
    - name: my-pvc-mount
      configMap:
        name: configmap
