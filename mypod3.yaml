apiVersion: v1
kind: Pod
metadata:
  name: mypodIII
spec:

  containers:
    - name: my-container
      image: my-image
      volumeMounts:
        - name: mypvcmountII
          mountPath: WORKDIR /app
  volumes:
    - name: mypvcmountII
     configMap:
        claimName: configmapII
